interface:
  display_name: "My App"
  short_description: "Repo-specific patterns and workflows for my-app"
  default_prompt: "Use the my-app repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true